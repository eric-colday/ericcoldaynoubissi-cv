import React from 'react';
import Navigation from '../components/Navigation';

const NotFound = () => {
    return (
        <div>
            Not found
        </div>
    );
};

export default NotFound;